name: Generate Snake Animation

on:
  # runs automatically every 12 hours
  schedule:
    - cron: "0 */12 * * *"
  # lets you run it manually from the Actions tab
  workflow_dispatch:
  # runs on every push to main (first run happens right after you commit this file)
  push:
    branches:
      - main

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      # builds the snake SVGs from your contribution graph
      - name: Generate snake SVGs
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-snake.svg
            dist/github-snake-dark.svg?palette=github-dark

      # pushes the SVGs to a branch called "output"
      - name: Push SVGs to the output branch
        uses: crazy-max/ghaction-github-pages@v3.1.0
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
